import React from "react";

function Box({ pastFixture,nextFixture  }) {
    console.log(pastFixture);
    console.log(nextFixture);
    return (
        <div>
            {/* <section>
                <h1>Previous Match</h1>
                <p>{pastFixture.teams.home.name} vs {pastFixture.teams.away.name}</p>
            </section> */}
        </div>
    );
    }
    export default Box;